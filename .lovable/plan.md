
# Plano: Proteger IOAudit com FullAdminRoute

## Alteração: `src/pages/admin/IOAudit.tsx`

1. **Linha 4**: Adicionar import de `FullAdminRoute` do `RoleRoute`
2. **Linha ~29 (return)**: Wrap do conteúdo com `<FullAdminRoute>...</FullAdminRoute>` — mesmo padrão de IOMissions

Duas edições pontuais, nenhum outro arquivo alterado.
